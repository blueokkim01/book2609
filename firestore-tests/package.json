{
  "name": "reading-challenge-rules-tests",
  "private": true,
  "description": "firestore.rules / storage.rules 에 대한 역할별 read/write 권한 테스트. Firebase 에뮬레이터(firestore, storage)가 실행 중이어야 한다.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-reading-challenge \"jest --runInBand\"",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.12",
    "firebase": "^10.12.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.5.3"
  }
}
